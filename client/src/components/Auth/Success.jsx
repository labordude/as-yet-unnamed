import React from 'react'

export default function Success({ newUser }) {
    const {name, username, password, email, bio, pfp_image} = newUser
  return (
      <div>Successfully created:
          <ul>{newUser.map</ul>
    </div>
  )
}
